oVirt Gentoo Overlay
====================

Drop the following content into /etc/portage/repos.conf/ovirt-overlay.conf

```
[ovirt-overlay]
location = /usr/local/portage/ovirt-overlay
sync-type = git
sync-uri = https://github.com/alonbl/ovirt-overlay
```
